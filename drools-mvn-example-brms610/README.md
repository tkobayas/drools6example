This is a simple example to demonstrate kjar <- main application dependency. So this is a simpler version of drools-simple-dep-example.

cd ../drools-mvn-kjar
mvn clean install
cd ../drools-mvn-main
mvn test
